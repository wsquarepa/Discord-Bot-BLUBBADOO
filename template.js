var userData = require('../userData.json')
const fs = require('fs');
const discord = require("discord.js")

module.exports = {
    name: '',
	description: '',
    args: false,
    usage: '',
    guildOnly: false,
    aliases: [],
    cooldown: 0,
	execute(message, args) {

    }
}